# Boundary Electron Wrapper

This wraps the local Boundary backend at `http://127.0.0.1:8787` in a desktop shell.

## Local run
From this folder:
- `npm install`
- `npm start`

## Build
- `npm run build-win`
- `npm run build-mac`
