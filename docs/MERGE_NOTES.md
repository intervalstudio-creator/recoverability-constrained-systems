# Merge Notes

This pack is structured to match the Boundary v4.1 project layout described in your uploaded architecture text:

- `engine/`
- `api/`
- `app/`
- `docs/`
- `config/`

Because the full source tree was not included here as editable code files, this pack is a merge-ready patch rather than a rewritten full project archive.

It is intended to be dropped into the existing Boundary v4.1 repository or project folder.

The result is:
- existing evaluation engine preserved
- optional downstream enforcement forwarding added
- no change to the core published statement that outputs require responsible human authority
